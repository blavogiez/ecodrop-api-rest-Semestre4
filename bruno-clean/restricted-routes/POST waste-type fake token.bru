meta {
  name: POST waste-type fake token - 401
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/waste-types
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{fakeToken}}
}

body:json {
  {
    "id": 99,
    "nom": "test fake token",
    "pointsPerKilo": 1.0
  }
}

tests {
  test("Status 401 - fake token rejeté", function() {
    expect(res.status).to.equal(401);
  });
}
