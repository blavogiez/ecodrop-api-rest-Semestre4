meta {
  name: DELETE point clear as user - 401
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/points/1/clear
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

tests {
  test("Status 401 - user ne peut pas DELETE", function() {
    expect(res.status).to.equal(401);
  });
}
