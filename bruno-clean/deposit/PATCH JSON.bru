meta {
  name: PATCH deposit JSON [Partie B]
  type: http
  seq: 13
}

patch {
  url: {{baseUrl}}/deposits/2
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "poids": 55.0,
    "collecte": true
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
