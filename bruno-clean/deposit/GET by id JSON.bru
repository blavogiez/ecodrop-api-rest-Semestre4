meta {
  name: GET deposit by id JSON
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/deposits/2
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
