meta {
  name: GET deposit by id JSON
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/deposit/2
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
