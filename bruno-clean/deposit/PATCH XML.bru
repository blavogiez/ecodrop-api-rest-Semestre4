meta {
  name: PATCH deposit XML [Partie B]
  type: http
  seq: 14
}

patch {
  url: {{baseUrl}}/deposits/2
  body: xml
  auth: none
}

headers {
  Content-Type: application/xml
  Accept: application/xml
  Authorization: Bearer {{token}}
}

body:xml {
  <deposit>
    <poids>7.0</poids>
  </deposit>
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
