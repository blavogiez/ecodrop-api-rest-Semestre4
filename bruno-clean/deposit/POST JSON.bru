meta {
  name: POST deposit JSON
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/deposits
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "userId": 1,
    "pointId": 3,
    "wasteTypeId": 1,
    "poids": 5.0,
    "datedepot": "2025-01-15",
    "collecte": "false"
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
