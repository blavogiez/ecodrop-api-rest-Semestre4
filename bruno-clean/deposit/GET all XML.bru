meta {
  name: GET all deposits XML
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/deposit
  body: none
  auth: none
}

headers {
  Content-Type: application/xml
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
