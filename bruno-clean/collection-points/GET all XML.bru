meta {
  name: GET all collection-points XML
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/points
  body: none
  auth: none
}

headers {
  Content-Type: application/xml
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
