meta {
  name: GET collection-point status JSON [Partie B]
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/points/1/status
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
