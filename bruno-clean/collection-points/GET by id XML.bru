meta {
  name: GET collection-point by id XML
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/points/1
  body: none
  auth: none
}

headers {
  Accept: application/xml
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
