meta {
  name: PATCH collection-point JSON
  type: http
  seq: 11
}

patch {
  url: {{baseUrl}}/points/1
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "adresse": "21 Rue DuChemin patchée JSON"
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
