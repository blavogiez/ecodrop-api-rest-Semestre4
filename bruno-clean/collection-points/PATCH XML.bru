meta {
  name: PATCH collection-point XML
  type: http
  seq: 12
}

patch {
  url: {{baseUrl}}/points/1
  body: xml
  auth: none
}

headers {
  Content-Type: application/xml
  Authorization: Bearer {{token}}
}

body:xml {
  <collectionPoint>
    <adresse>21 Rue DuChemin patchée XML</adresse>
  </collectionPoint>
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
