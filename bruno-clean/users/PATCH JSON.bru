meta {
  name: PATCH user JSON
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/users/4
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "login": "temoin-patched"
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
