meta {
  name: GET leaderboard XML
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/users/leaderboard
  body: none
  auth: none
}

headers {
  Content-Type: application/xml
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
