meta {
  name: PATCH user XML
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/users/4
  body: xml
  auth: none
}

headers {
  Content-Type: application/xml
  Authorization: Bearer {{token}}
}

body:xml {
  <users>
    <login>temoin-patched-xml</login>
  </users>
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
