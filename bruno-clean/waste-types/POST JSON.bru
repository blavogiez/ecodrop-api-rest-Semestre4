meta {
  name: POST waste-type JSON
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/waste-types
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "id": 99,
    "nom": "test déchets JSON",
    "pointsPerKilo": 3.5
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
