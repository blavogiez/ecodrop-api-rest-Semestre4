meta {
  name: GET waste-type by id JSON
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/waste-types/2
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
