meta {
  name: GET all waste-types XML
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/waste-types
  body: none
  auth: none
}

headers {
  Content-Type: application/xml
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
