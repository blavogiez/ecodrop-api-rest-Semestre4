meta {
  name: DELETE waste-type as user - 401
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/waste-types/1
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

tests {
  test("Status 401 - user ne peut pas DELETE", function() {
    expect(res.status).to.equal(401);
  });
}
