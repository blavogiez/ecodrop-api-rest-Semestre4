meta {
  name: GET waste-type by id XML
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/waste-types/2
  body: none
  auth: none
}

headers {
  Accept: application/xml
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
