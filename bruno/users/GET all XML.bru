meta {
  name: GET all users XML
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/users
  body: none
  auth: none
}

headers {
  Accept: application/xml
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
