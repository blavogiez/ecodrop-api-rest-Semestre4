meta {
  name: GET deposit by id XML
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/deposits/2
  body: none
  auth: none
}

headers {
  Accept: application/xml
  Authorization: Bearer {{token}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
