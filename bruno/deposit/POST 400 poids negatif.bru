meta {
  name: POST deposit 400 poids negatif [Partie B]
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/deposits
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "userId": 1,
    "pointId": 1,
    "wasteTypeId": 1,
    "poids": -5.0,
    "datedepot": "2025-01-15",
    "collecte": "false"
  }
}

tests {
  test("Status 400", function() {
    expect(res.status).to.equal(400);
  });
}
