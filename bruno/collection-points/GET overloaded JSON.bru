meta {
  name: GET overloaded collection-points JSON [Partie B]
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/points/overloaded
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{tokenAdmin}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
