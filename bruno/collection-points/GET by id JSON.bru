meta {
  name: GET collection-point by id JSON
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/points/1
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
